--- a/configure.ac
+++ b/configure.ac
@@ -164,6 +164,9 @@ if test $can_printf_longlong = yes; then
         AC_DEFINE(HAVE_QUAD_SUPPORT, 1)
 fi
 
+AC_DEFINE(HAVE_QUAD_SUPPORT, 1)
+
+
 dnl ===================================================================
 dnl Check for compiler characteristics
 
